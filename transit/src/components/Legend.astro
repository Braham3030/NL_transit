---
---

<div class="Legend">
    <details>
    <summary>Legend</summary>
    <ul>
        <li><strong>Stops:</strong> Stops for transit. <div class="stopsLegend"></div></li>
        <li><strong>Bus:</strong> A bus route.</li>
        <li><strong>Subway:</strong> A subway route.</li>
        <li><strong>Tram:</strong> A tram route.</li>
    </ul>
    </details>
</div>

<style>
    .Legend {
        position: fixed;
        display: flex;
        align-items: flex-end;
        justify-content: flex-end;
        bottom: 8.5em;
        right: 1.3em;
        transform: translate(-20%, -50%);
        z-index: 9999;
        width: auto;
        height: auto;
        background-color: transparent;
        padding: 0;
        border-radius: 0.5em;

        transition: transform 0.8s cubic-bezier(0.33, 0.72, 0, 1.18), bottom 0.8s cubic-bezier(0.33, 0.72, 0, 1.18), right 0.8s cubic-bezier(0.33, 0.72, 0, 1.18), margin-top 0.8s cubic-bezier(0.33, 0.72, 0, 1.18), opacity 0.8s cubic-bezier(0.33, 0.72, 0, 1.18);

        > details {
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            padding: 0.5em;
            border-radius: 16px;
            width: 7em;
            height: 3.5em;

            color: white;
            background-color: rgba(0, 0, 0, 0.6);
            box-shadow: 0 2px 20px rgba(0, 0, 0, 0.3);
            backdrop-filter: blur(6px);
            position: absolute;
            bottom: 0;
            right: 0;
            transform: translate(0, 0);
            transform-origin: bottom right;

            transition: width 0.8s cubic-bezier(0.33, 0.72, 0, 1.18), height 0.6s cubic-bezier(0.33, 0.72, 0, 1.18), bottom 0.8s cubic-bezier(0.33, 0.72, 0, 1.18), right 0.8s cubic-bezier(0.33, 0.72, 0, 1.18);

            animation: Shrink 0.8s cubic-bezier(0.33, 0.72, 0, 1.18) forwards;

            > summary {
                font-size: 22px;
                font-weight: bold;
                cursor: pointer;
                list-style-type: none;
                padding-top: 0.2em;
                padding-bottom: 0.2em;

                transition: all 0.8s cubic-bezier(0.33, 0.72, 0, 1.18);
            }

        > ul {
            display: flex;
            flex-direction: column;
            gap: 0em;
            list-style-type: none;
            padding: 0;
            margin: 0;
            margin-top: 0em;

            /* cubic-bezier(0.33, 0.72, 0, 1.18) forwards */

            > li {
                display: flex;
                align-items: center;
                justify-content: flex-start;
                flex-direction: row;
                gap: 0.5em;

                font-size: 14px;

                transition: all 0.8s cubic-bezier(0.33, 0.72, 0, 1.18);

                > .stopsLegend {
                    width: 1em;
                    height: 1em;
                    border-radius: 50%;
                    border: 2px solid white;
                    background-color: #3388ff;
                }
            }
        }
    }

    > details[open] {
        width: 20em;
        height: 13em;
        bottom: 0;
        right: 0;
        transform: translate(0, 0);
        align-items: flex-start;
        justify-content: flex-start;

        background-color: rgba(0, 0, 0, 0.8);
        /* height: auto; */
        transition: background-color 0.8s cubic-bezier(0.33, 0.72, 0, 1.18), width 0.6s cubic-bezier(0.33, 0.72, 0, 1.18), height 0.8s cubic-bezier(0.33, 0.72, 0, 1.18), bottom 0.8s cubic-bezier(0.33, 0.72, 0, 1.18), right 0.8s cubic-bezier(0.33, 0.72, 0, 1.18);
        animation: enLarge 0.8s cubic-bezier(0.33, 0.72, 0, 1.18) forwards;

        > summary {
            font-size: 26px;

            transition: all 0.8s cubic-bezier(0.33, 0.72, 0, 1.18);
        }

        > ul {
            gap: 0.3em;
            margin-top: 1em;

            > li {
                font-size: 20px;

                transition: all 0.8s cubic-bezier(0.33, 0.72, 0, 1.18);
            }
        }
    }
}

@keyframes enLarge {
    from {
        width: 7em;
    }
    to {
        width: 20em;
    }
}

@keyframes Shrink {
    from {
        width: 20em;
    }
    to {
        width: 7em;
    }
}
</style>